# .claude/skills/

Skills graduated from `mistakes.md`. Each is `<name>/SKILL.md` with frontmatter
`name:` and `description:` (the trigger), then the corrected procedure. Keep each
under 100 lines. Record the graduation in `log.md` as `* **Skill**: …`.
